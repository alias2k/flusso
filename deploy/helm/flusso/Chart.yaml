apiVersion: v2
name: flusso
description: Keep OpenSearch in sync with Postgres from declarative config.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# The flusso version this chart deploys by default (the image tag).
appVersion: "0.1.0"
home: https://github.com/OWNER/flusso
sources:
  - https://github.com/OWNER/flusso
keywords:
  - opensearch
  - elasticsearch
  - postgres
  - cdc
  - search
  - replication
maintainers:
  - name: flusso
